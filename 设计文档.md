# 宝宝拼图乐 — 完整开发规格说明书（Codex-Ready）

> **目标**：生成一个可直接运行的单文件 HTML 游戏，专为 3–6 岁儿童设计，无需任何外部资源（图片通过 Canvas 绘制或内嵌 SVG，音效通过 Web Audio API 合成）。  
> **核心原则**：零挫败感、大操作区、强引导、无文字依赖。

---

## 一、技术栈与文件结构

- **单文件 HTML**，包含 `<style>` 和 `<script>`。
- **移动优先**，强制竖屏，使用 `meta viewport`。
- **交互**：统一使用 **Pointer Events**（`pointerdown/pointermove/pointerup`），兼容触屏与鼠标。
- **音效**：使用 `AudioContext` 生成简短合成音。
- **无外部依赖**，不使用任何字体、图片或音频文件。
- **图片素材**：内置 6 张纯色可爱动物面部特写，用 **Canvas 动态绘制**（正方形，主体突出，背景简洁）。
- **布局单位**：使用 `vmin` 确保正方形图块在所有屏幕上都足够大（最小点击区域 ≥ 64px 逻辑像素，实际约 20vmin）。

---

## 二、界面布局（竖屏）

```
┌─────────────────────────┐
│  [🏠]   [参考小图]  [🔄] │ ← 顶部工具栏 (高度 10vmin)
├─────────────────────────┤
│                         │
│   ┌───┬───┬───┐       │
│   │   │   │   │       │
│   ├───┼───┼───┤       │ ← 3×3 目标网格 (每个格子 25vmin × 25vmin)
│   │   │   │   │       │    半透明底图 (alpha=0.25)
│   ├───┼───┼───┤       │
│   │   │   │   │       │
│   └───┴───┴───┘       │
├─────────────────────────┤
│   碎片区 (3x3 固定网格)   │ ← 每个碎片 25vmin，同样采用网格排列，碎片不可重叠
│  ┌───┬───┬───┐        │
│  │ P0│ P1│ P2│        │
│  ├───┼───┼───┤        │
│  │ P3│ P4│ P5│        │
│  ├───┼───┼───┤        │
│  │ P6│ P7│ P8│        │
│  └───┴───┴───┘        │
├─────────────────────────┤
│ [🧸 小帮手]            │ ← 辅助按钮，剩余次数显示为小熊胸前的数字气泡
└─────────────────────────┘
```

- **参考小图**：屏幕右上角，圆形缩略图（直径 8vmin），点击可全屏预览原图。
- **导航按钮**：🏠(主页) 和 🔄(换图) 均需 **长按 1 秒** 或二次确认（弹出表情确认框）。

---

## 三、核心数据结构

```javascript
// 游戏状态
const state = {
  currentImageId: 0,          // 当前图片索引 (0-5)
  grid: [null, null, ...],   // 3x3 目标网格，共9个元素，null 或 碎片的原始索引(0-8)
  pieces: [0,1,2,3,4,5,6,7,8], // 碎片区从左到右、从上到下的碎片原始索引 (初始随机排列)
  helperRemaining: 3,        // 小帮手剩余次数
  isDragging: false,
  dragPieceIndex: null,      // 当前正在拖拽的碎片在 pieces[] 中的原始索引
  dragStartX: 0, dragStartY: 0, // 拖拽起始点（用于飞回动画）
  longPressTimer: null,      // 长按计时器
  lockUI: false,             // 动画锁，防止快速操作
  completed: false           // 是否已完成拼图
};
```

**图片素材定义**：使用 Canvas 动态绘制 6 张动物脸部特写（猫、狗、兔、熊、熊猫、小猪）。主体颜色鲜明，背景为纯色浅色。每张图片为 600×600 像素的 Canvas，切割后每块 200×200。

**切割逻辑**：将完整图片等分为 3 行 3 列，碎片索引顺序为：
```
0 1 2
3 4 5
6 7 8
```

---

## 四、交互流程与状态机

### 4.1 游戏初始化
- 随机选择 `currentImageId`，或从选图界面进入。
- 在离屏 Canvas 绘制完整图片，切割成 9 个 `ImageData` 或独立的小 Canvas 对象，保存为 `pieceCanvases[0..8]`。
- 随机打乱 `pieces` 数组（Fisher–Yates 洗牌）。
- 重置 `grid` 为全 null，`helperRemaining = 3`，`completed = false`。
- 渲染目标网格底图（每个格子显示半透明的对应碎片图）和碎片区。

### 4.2 拖拽放置机制（核心）

**使用 Pointer Events**，统一处理触摸与鼠标。

1. **pointerdown** 在碎片上：
   - 如果 `lockUI` 或碎片已在网格中（即 `grid` 中已存在该索引），忽略。
   - 记录 `dragPieceIndex`，碎片轻微放大（scale 1.1），增加阴影，提升 `z-index`。
   - 播放短促“啵”音效（可选）。
   - 记录碎片原始位置（用于飞回）。

2. **pointermove**：
   - 更新碎片位置跟随指针，碎片呈现**不透明**（原方案评审优化：拖拽中的碎片必须完全不透明且放大，带有清晰投影）。
   - 高亮检测：计算碎片中心点所在的目标网格格子索引（`targetCol = Math.floor((clientX - gridLeft) / cellSize)`等），如果该格子存在且 `grid[targetIndex] === null`，则将该格子边框变为**绿色粗边**并带有内部光晕。

3. **pointerup**：
   - 获取手指抬起时的目标格子索引。
   - 如果目标格子有效且为空：
     - 判断是否为正确位置：`grid[targetIndex] === null` 且 `dragPieceIndex === targetIndex`（注意：`dragPieceIndex` 是碎片的原始正确位置索引，而目标网格的索引应与碎片索引一致才正确）。
     - **正确放置**：执行“成功放置”流程（见下方）。
     - **错误放置**：执行“错误回弹”流程（见下方）。
   - 否则（未在任何格子释放）：
     - 同样执行“错误回弹”流程（同错误放置）。

#### 成功放置流程
1. 更新 `grid[targetIndex] = dragPieceIndex`。
2. 碎片瞬间吸附到格子中心，播放弹跳动画（CSS `transition` 或手动动画）。
3. 播放“咔哒”音效 + 成功振动反馈（如果支持）。
4. 触发小星星粒子特效（在格子中心短暂出现旋转星形）。
5. 检查胜利条件：
   - 若 `grid` 全部非空，触发“全部完成”庆祝。
   - 否则，仅提供轻微反馈（如该格子短暂发亮）。

#### 错误放置流程
1. 目标格子红色闪一下（0.2s），然后碎片执行**平滑飞回动画**，从当前位置飞回到碎片区的原始位置。
2. 同时播放轻柔下滑音“唔~”。
3. 飞回期间 `lockUI = true`，动画完成后解锁。
4. 碎片恢复原始大小和透明度。

### 4.3 小帮手功能（优化方案）
- 点击“🧸 小帮手”按钮时，如果 `helperRemaining > 0` 且游戏未完成，按钮开始脉动，进入 **“选择碎片”模式**。
- 此时界面显示提示：小帮手图标上出现一个“？”，等待儿童点击碎片区的任意一块碎片。
- 当儿童点击某块未放置的碎片后：
  - 消耗一次 `helperRemaining--`，更新按钮上的数字。
  - 该碎片自动以动画形式**直线飞到**其正确网格位置，整个过程无需儿童拖拽。
  - 自动执行成功放置后的所有反馈（音效、吸附、星星等）。
  - 退出“选择碎片”模式。
- 若在模式中点击其他区域，则取消模式（不消耗次数）。

### 4.4 胜利庆祝
当 `grid` 全部填满时：
1. `completed = true`。
2. 播放礼花动画：全屏随机彩色圆点从底部喷涌（CSS animation 或 Canvas 粒子）。
3. 持续 5 秒星星飘落（使用 CSS 动画生成多个星形 emoji 从顶部飘落）。
4. 播放欢快旋律（合成音阶） + 掌声音效 + 语音“你真棒！”（用 Web Speech Synthesis 或预合成的短语音，或干脆省略语音只保留音乐和掌声）。
5. 显示一个巨大的“🎉” emoji 居中弹出。
6. 3 秒后，出现两个选项按钮（图标）：🔄 再玩一次 | 🏠 主页（均为普通点击，无需长按）。

---

## 五、视觉与动效规格

### 5.1 色彩与圆角
- 主背景：柔和渐变色 `linear-gradient(135deg, #FFE68F, #B0E2FF)`。
- 图块边框：圆角 `12px`，白色描边 `3px`，`box-shadow: 0 4px 8px rgba(0,0,0,0.15)`。
- 目标网格格子：背景为半透明底图，圆角 `12px`，内部无边框，正常状态边界为虚线浅灰色。
- 碎片区：底色淡白半透明，圆角 `16px`，内边距。

### 5.2 半透明底图
- 每个目标格子用 `opacity: 0.25` 显示对应索引的碎片图片。图片完全覆盖格子区域，`object-fit: cover`。
- **评审优化**：为避免拖拽时视觉混乱，拖拽中的碎片必须 `opacity: 1`，`transform: scale(1.1)`，并应用 `filter: drop-shadow(0 8px 12px rgba(0,0,0,0.3))`，确保它从底图中强烈凸显。

### 5.3 引导提示
- 点击碎片区中未放置的碎片时，其正确网格位置应闪烁金色边框（`@keyframes blink-border`，1秒2次）持续1.5秒。（这需要在碎片上添加 click 事件监听，但要防止与拖拽冲突：若轻点碎片（没有明显移动），则触发提示闪烁；若拖拽超过阈值，则不触发提示。）

### 5.4 动画库
- 成功弹跳：`transition: transform 0.2s cubic-bezier(0.68, -0.55, 0.265, 1.55);`
- 飞回动画：动态使用 `Element.animate()` 或 CSS `transition` 移动坐标，持续时间 0.4s，缓入缓出。
- 礼花：使用 CSS 粒子类，生成 50 个绝对定位的圆形 div，随机颜色、大小，从中心向外爆炸并淡出。

### 5.5 按钮设计
- 所有图标按钮至少 64x64px 可视区域，使用 `font-size: 2.5rem` 的 emoji。
- 长按按钮逻辑：`pointerdown` 启动 1000ms 计时器，`pointerup/pointercancel` 清除。若计时器完成，执行操作，并且播放确认音。
- 二次确认弹窗：一个简单的白色圆角卡片，显示 emoji 问题（如“❓”），两个按钮 👍 和 👎。

---

## 六、音效合成规格（Web Audio API）

使用 AudioContext 创建如下音效：

| 音效 | 描述 | 实现方式 |
|------|------|----------|
| 咔哒（正确） | 短促清脆 | 带通噪声或方波，快速衰减，频率 800Hz，时长 0.08s |
| 唔~（错误） | 下滑音 | 正弦波，频率从 400Hz 滑到 200Hz，时长 0.25s |
| 叮咚（一行完成） | 双音符上升 | 两个短正弦波，800Hz 和 1200Hz，各 0.1s |
| 完成旋律 | 欢快简单 | 用 5 个音符逐渐上升（C D E F G），每个 0.15s，然后一个小号音色（近似） |
| 礼花 | 持续噼啪 | 多个短噪声片段随机间隔播放 |

使用简单的函数 `playTone(freq, type, duration, volume)` 封装。

---

## 七、图片素材动态生成

使用离屏 Canvas 绘制 6 种动物面部。绘画函数示例（伪代码）：
- `drawCatFace(ctx, size)`：橙色圆脸，三角形耳朵，大眼睛，胡须。
- `drawDogFace(ctx, size)`：棕色圆脸，垂耳，鼻子，吐舌。
- 依此类推，确保每个图形**主体占画布 80%**，背景为柔和纯色（如淡黄、淡粉），轮廓清晰，颜色对比强。

所有图形使用基本形状（圆、弧、贝塞尔曲线）即可，无需复杂细节。

---

## 八、额外功能与细节

- **防误触主页/换图**：🏠 和 🔄 必须长按 1 秒，通过视觉进度环表示长按进度。
- **换图**：弹出确认框（显示“🤔？”和 👍/👎），确认后重置拼图。
- **屏幕旋转锁**：使用 CSS `@media (orientation: landscape) { body { display: flex; justify-content: center; align-items: center; } }` 提示用户旋转设备。或强制使用竖屏布局。
- **响应式**：网格和碎片尺寸使用 `calc((100vmin - 40px) / 3)` 左右，确保 320px 宽屏幕每个格子也在 64px 以上。

---

## 九、代码结构要求

生成最终的 HTML 文件结构：
1. `<!DOCTYPE html>`
2. `<meta>` viewport, 禁止缩放。
3. `<style>` 包含所有布局、动画、按钮样式。
4. `<body>` 包含界面 DOM 结构。
5. `<script>`：
   - 辅助函数：音效、Canvas 绘图、洗牌。
   - 游戏状态与初始化。
   - 渲染函数：`renderGrid()`, `renderPieces()`, `updateHelperButton()`.
   - 事件处理：Pointer Events 实现拖拽、碎片点击提示、小帮手模式。
   - 庆祝动画管理。
6. 整个脚本包裹在 `DOMContentLoaded` 或直接作为模块。

**最重要的是**：代码需完全支持移动端触屏，防止滚动、缩放干扰（`touch-action: none` 在交互元素上）。

---
